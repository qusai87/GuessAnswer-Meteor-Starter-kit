Template.ask.events({
	// "submit form": function (event) {
	// 	event.preventDefault();
	// 	if (Meteor.userId()) {

	// 		Meteor.call('addQuestion', $('[name=title]').val() , function(err, response) {
	// 			if (!err)
	// 				Router.go("/answers/" + response);
				
	// 		});

	// 		$('[name=title]').val('');
			
	// 	}
	// }
});

// Template.ask.onRendered(function() { 
//   $('#summernote').summernote();
// });